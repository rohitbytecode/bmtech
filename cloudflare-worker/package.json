{
  "name": "bmtech-crawler-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for BMTech Crawler",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "tsc --noEmit",
    "format": "prettier --write src/**/*.ts"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "cheerio": "^1.0.0-rc.12"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240902.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.0.0"
  }
}
